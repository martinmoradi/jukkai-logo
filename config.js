// URL of the Cloudflare Worker that stores the hidden-image selection.
// Empty = the selection stays on this device only (localStorage).
// Filled in once the Worker in worker/ is deployed.
window.SYNC_URL = '';
